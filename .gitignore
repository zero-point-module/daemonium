# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build
/plan
CLAUDE.md

# local tooling & source assets (kept local, not shared)
/.agents
skills-lock.json
/references

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (secrets live here — never commit; see .env.example)
.env
.env*.local

# daemon store — non-sensitive wallet/handle index (metadata only, no key material); for local dev
/.daemon/

# vercel
.vercel
